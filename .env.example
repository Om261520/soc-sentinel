# Database URL Configuration (SQLite default / PostgreSQL production)
DATABASE_URL=sqlite:///./soc_sentinel.db

# JWT Security Key Configuration
JWT_SECRET_KEY=soc_sentinel_super_secret_jwt_key_2026_change_in_prod
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS Allowed Origins
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional AI Module OpenAI Key (Fallback deterministic engine used if omitted)
# OPENAI_API_KEY=sk-...
